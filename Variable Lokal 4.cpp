#include <stdio.h>
#include <conio.h>
int tambah(int a, int b);
int main()
{
    int hasil;
    hasil = tambah(2,3);
    printf("hasil=%d",hasil);
    getch();
}
int tambah (int a, int b)
{
    int c;
    c = a=b;
    {
        float c;
        c=100;
    }
return(c);
}
